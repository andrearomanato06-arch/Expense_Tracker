<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Connessione al DB -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/expense_tracker</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">4nt4n1</property>

        <!-- Dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MariaDBDialect</property>

        <!-- Creazione Tabelle -->
        <property name="hibernate.hbm2ddl.auto">update</property>
        
        <!-- Log SQL -->
        <property name="show_sql">true</property>

        <!-- Le entità da gestire -->
        <mapping class="aret.entities.Expense"/>

    </session-factory>
</hibernate-configuration>
